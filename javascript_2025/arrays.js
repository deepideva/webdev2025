/*var home = ["Ganesh","Kalai","Deepika","Devasree"];
console.log(home);
console.log(home[1]);
console.log(home[0],home[1]);

// Get names from Users and check

// Prompt user to enter 5 names separated by commas
var names = prompt("Enter 5 names (comma-separated) and no space after Comma").split(",");
alert("Thanks for Names., Data Saved");
var newname = prompt("Check the names you added");
if (names.includes(newname)){
    alert("This name already stored")
} else { 
    alert("Sorry, No new entries allowed")
}


// Log the array to the console
console.log(names);
console.log(names[1]);
*/


